# Optional: real 1Password runtime credential broker (Service Account + SDK).
# Install AFTER requirements.txt:  pip install -r requirements-1password.txt
# Without it (or without OP_SERVICE_ACCOUNT_TOKEN), the broker uses a mock
# secret and the UI/audit honestly report credential_live = false.
onepassword-sdk==0.4.0
