# SpendGuard backend dependencies (Python 3.11+)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
certifi>=2024.0.0
langgraph==0.2.60
langchain-core==0.3.28

# LLM (optional at runtime — intent extraction falls back to deterministic parsing)
openai==1.59.6

# Dev / tests
pytest==8.3.4
httpx==0.28.1

# NOTE: the Daytona SDK is intentionally kept OUT of this core file so the core
# install never breaks. Install it separately to enable the real cloud sandbox:
#     pip install -r requirements-daytona.txt
# Without it, SpendGuard automatically uses the local executor and every UI
# field (executor, sandbox id, credential source, secret-visible-to-model) is
# preserved.
